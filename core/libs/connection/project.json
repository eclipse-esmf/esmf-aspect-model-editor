{
  "name": "connection",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/connection/src",
  "prefix": "ame",
  "targets": {
    "test": {
      "executor": "@angular/build:unit-test",
      "outputs": ["{workspaceRoot}/coverage/libs/connection"],
      "options": {
        "tsConfig": "libs/connection/tsconfig.spec.json",
        "buildTarget": "connection:build",
        "watch": false
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "build": {
      "executor": "@angular/build:ng-packagr",
      "outputs": ["{workspaceRoot}/dist/libs/connection"],
      "options": {
        "project": "libs/connection/ng-package.json"
      },
      "dependsOn": []
    }
  }
}
