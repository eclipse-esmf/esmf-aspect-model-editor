{
  "name": "electron",
  "root": "electron",
  "sourceRoot": "electron",
  "projectType": "application",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/electron"],
      "options": {
        "command": "vitest run --config electron/vitest.config.ts --passWithNoTests",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
